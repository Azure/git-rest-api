{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Run server",
      "program": "${workspaceFolder}/src/main.ts",
      "outFiles": ["${workspaceFolder}/bin/**/*.js"],
      "console": "integratedTerminal",
      "sourceMaps": true,
      "cwd": "${workspaceFolder}"
    }
  ]
}
